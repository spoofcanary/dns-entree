package main

// Marker file -- the fake provider is registered in fakeprovider.go so that
// the subprocess-built binary used by integration tests also carries the fake
// slug. Keeping an empty _test.go here preserves the plan's filename.
